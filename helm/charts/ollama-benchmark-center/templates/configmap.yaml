apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "obc.name" . }}-config
  labels:
    {{- include "obc.labels" . | nindent 4 }}
data:
  OLLAMA_BASE_URL: {{ .Values.ollama.baseUrl | quote }}
  CORS_ORIGINS: "http://localhost:3000"
