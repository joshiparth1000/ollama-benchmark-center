apiVersion: v1
kind: Service
metadata:
  name: {{ include "obc.name" . }}-backend
  labels:
    {{- include "obc.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/component: backend
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: 8000
