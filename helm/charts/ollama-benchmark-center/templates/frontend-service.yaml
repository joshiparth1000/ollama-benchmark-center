apiVersion: v1
kind: Service
metadata:
  name: {{ include "obc.name" . }}-frontend
  labels:
    {{- include "obc.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/component: frontend
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: 80
